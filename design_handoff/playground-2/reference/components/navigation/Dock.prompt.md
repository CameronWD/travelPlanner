Desktop left dock — a 96px sun-coloured column with the mark on top, text tabs, and collaborators at the bottom.

```jsx
<Dock items={[{ key: 'home', label: 'Home' }, { key: 'plan', label: 'Plan' }]} value="plan" people={[{ initials: 'CW' }]} />
```

Active item = coral sticker. The dock is the only coloured chrome on desktop; content area is paper.
