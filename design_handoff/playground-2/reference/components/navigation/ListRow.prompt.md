Task / list row with a coloured glyph tile, two lines of text and a trailing arrow.

```jsx
<ListRow tile="Zz" title="Hakone needs a bed" sub="1 night · Oct 17" />
```

Rows sit inside a white Card with 10–12px vertical gaps; no dividers.
