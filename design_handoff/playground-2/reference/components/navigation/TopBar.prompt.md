Mobile page header: big Bricolage title with optional leading/trailing controls.

```jsx
<TopBar title="The plan" trailing={<Select size="s" value="Real plan" options={['Real plan']} />} />
```

Titles are short, lowercase-first-word sentences: "The plan", "Your trips", "Sort these out".
