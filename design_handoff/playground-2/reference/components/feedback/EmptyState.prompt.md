Dashed empty slot with a tilted sticker tile, a headline and one action.

```jsx
<EmptyState glyph="Zz" tone="lilac" title="No beds yet" body="Every stop needs somewhere to sleep." action="+ Add a stay" />
```

Empty states are invitations, not errors: "Nothing here yet — start with where."
