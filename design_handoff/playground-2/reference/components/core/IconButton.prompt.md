Square, rounded icon button (44px hit target) with outline + hard shadow.

```jsx
<IconButton label="Back">‹</IconButton>
<IconButton tone="ink" label="Add"><Icon name="plus" color="var(--text-inverse)" /></IconButton>
```

Never below 40px on touch. Ghost tone for back arrows in top bars.
