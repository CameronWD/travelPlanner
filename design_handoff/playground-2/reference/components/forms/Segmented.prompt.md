Pill segmented control — Month / Week, Real plan / Fork.

```jsx
<Segmented options={['Month', 'Week']} value={view} onChange={setView} />
```

Max 4 options. Selected segment gets the outline + colour; others are muted text.
