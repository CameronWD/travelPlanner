Outlined text field; lifts with a hard shadow when focused.

```jsx
<Input label="Where to?" placeholder="Japan" value={v} onChange={setV} />
```

Focus = the field pops up (translate −2/−2 + shadow-2). Placeholders are conversational ("Somewhere warm?").
