Native select dressed as an outlined field, or a small pill dropdown for headers.

```jsx
<Select size="s" value="Real plan" options={['Real plan', 'Slow Kyoto']} />
```

The small pill variant is the fork switcher next to page titles.
