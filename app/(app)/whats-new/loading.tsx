import { ListSkeleton } from "@/components/ui/skeletons";

/** Loading state for the what's new page. */
export default function WhatsNewLoading() {
  return <ListSkeleton label="Loading what's new" />;
}
