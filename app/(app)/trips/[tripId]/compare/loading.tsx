import { DetailSkeleton } from "@/components/ui/skeletons";

/** Loading state for the Compare plans page. */
export default function CompareLoading() {
  return <DetailSkeleton label="Loading comparison" />;
}
