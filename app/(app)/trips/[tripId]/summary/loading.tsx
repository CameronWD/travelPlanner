import { DetailSkeleton } from "@/components/ui/skeletons";

/** Loading state for the Summary page. */
export default function SummaryLoading() {
  return <DetailSkeleton label="Loading summary" />;
}
