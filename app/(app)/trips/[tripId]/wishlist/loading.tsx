import { ListSkeleton } from "@/components/ui/skeletons";

/** Loading state for the Wishlist page. */
export default function WishlistLoading() {
  return <ListSkeleton label="Loading wishlist" />;
}
