import { FormSkeleton } from "@/components/ui/skeletons";

/** Loading state for the Settings page. */
export default function SettingsLoading() {
  return <FormSkeleton label="Loading settings" />;
}
