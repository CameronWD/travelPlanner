import { DetailSkeleton } from "@/components/ui/skeletons";

/** Loading state for the printable itinerary. */
export default function PrintLoading() {
  return <DetailSkeleton label="Loading printable itinerary" />;
}
